## 1. 分割中如何处理样本类别不均衡的情况？
- 损失函数加权（如加大少数类权重，常用加权交叉熵、Dice Loss、Focal Loss等）
- 数据增强（如对少数类样本进行CutMix、Mix-UP、Copy-paste等等）
- 在每轮训练中，使用类别平衡采样策略对样本进行选取

## 2. CNN和Transformer在处理分割问题时各具有什么优点和缺点？
- **CNN优点**：局部特征提取能力强，参数少，计算高效，适合小样本和边缘细节分割  
- **CNN缺点**：感受野有限，难以捕获全局依赖  
- **Transformer优点**：全局建模能力强，能捕获长距离依赖，适合大规模数据  
- **Transformer缺点**：计算和显存消耗大，对数据量要求高，训练慢

## 3. 全监督、半监督、弱监督语义分割任务区别是什么？半监督语义分割有哪些主流方法？一致性正则化的工作原理是什么？
- **全监督**：所有训练样本都有像素级标注  
- **半监督**：只有部分样本有像素级标注，其余为无标注或弱标注  
- **弱监督**：仅有图像级、框级或点级等粗标注  
- **主流半监督方法**：伪标签、对比学习、一致性正则化等  
- **一致性正则化原理**：对同一输入施加不同扰动（如数据增强），模型输出应保持一致，从而利用未标注数据提升泛化能力

## 4. RGB-T语义分割为什么分别使用非参数共享的双流特征提取器分别对RGB图像和热图像的特征进行提取？这样做的优缺点是什么？在模态融合时，使用简单的数值相加具有什么缺点？
- **原因**：RGB和热红外图像的特征分布和信息类型差异大，分别提取能更好适应各自模态特性  
- **优点**：能充分挖掘各模态独特信息，提升融合效果  
- **缺点**：参数量大，计算资源消耗高，训练难度增加  
- **简单数值相加的缺点**：无法区分不同模态特征的重要性，易导致信息互相干扰，融合效果有限

---

## 5. 参考图像分割与传统的语义分割或实例分割最主要的区别是什么？
最主要的区别在于分割依据的驱动信号。  
- 传统语义分割基于预定义的类别标签对图像中所有像素进行分类  
- 传统实例分割不仅区分类别，还要区分同一类别下的不同个体  
- 参考图像分割的分割依据是一个自由形式的自然语言描述，分割目标由语言动态指定，而不是固定的类别集合

## 6. 参考图像分割面临的主要挑战有哪些？（至少列举两个）
- 语言歧义性：自然语言描述可能模糊不清或有歧义，模型需要准确理解语言意图
- 视觉-语言对齐：模型需将语言描述中的词汇含义精准对应到图像中的视觉特征和空间关系上
- 处理复杂场景和遮挡：在复杂图像中精确定位和分割目标区域困难
- 细粒度理解：描述可能要求区分非常相似物体的细微差别，或分割非常细小的部分

## 7. 与静态图像语义分割相比，视频语义分割面临的核心挑战是什么？为什么？
核心挑战是保持时间一致性（Temporal Consistency），原因包括：
- 目标运动/相机运动导致相邻帧间目标位置和形状变化
- 运动模糊/遮挡
- 计算效率问题
- 帧间抖动，导致分割结果在时间轴上出现不连续、闪烁或“抖动”现象

## 8. 视频语义分割模型通常采用哪些关键技术来利用时序信息和提高效率？（至少列举两个）
- 光流/运动估计：计算相邻帧间的像素级运动，用于将前一帧的分割信息或特征“扭曲”到当前帧
- 时序特征聚合：使用RNN、3D CNN、Transformer或卷积LSTM等模块，在特征层面融合多帧信息
- 特征传播：设计特定模块将历史帧特征高效传播到当前帧
- 关键帧策略：对关键帧进行完整计算，非关键帧利用传播信息轻量处理
